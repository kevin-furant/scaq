version: '3'

services:

  mysql:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: 123456
      MYSQL_DATABASE: taskdb
    ports:
      - "3306:3306"

  fastapi:
    build: .
    depends_on:
      - mysql
    ports:
      - "8000:8000"